<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Duration x:Key="TextBlock_Anim_Duration">0:0:0.2</Duration>

    <Style TargetType="TextBlock" x:Key="BaseTextBlockStyle">
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="IconTextBlockStyle">
        <Setter Property="FontSize" Value="21"/>
        <Setter Property="FontWeight" Value="Light"/>
    </Style>

    <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}"/>

    <Style TargetType="TextBlock" x:Key="FloatingLabelStyle" BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="0"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="GotFocus">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                            To="-200" Duration="{StaticResource TextBlock_Anim_Duration}"/>
                        <DoubleAnimation Storyboard.TargetProperty="FontSize" 
                            To="12" Duration="{StaticResource TextBlock_Anim_Duration}"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

            <EventTrigger RoutedEvent="LostFocus">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                            To="0" Duration="{StaticResource TextBlock_Anim_Duration}"/>
                        <DoubleAnimation Storyboard.TargetProperty="FontSize"
                            To="16" Duration="{StaticResource TextBlock_Anim_Duration}"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>